<% layout('layouts/boilerplate') %>
  <div class="container-fluid mt-3">
<% cats.forEach(function(cat) {%> 
  <h1 class="text-center color fw500 fs-1 my-5"><%=cat.nume%></h1> 
  <% cat.produs.forEach(function(produs, i){%> 
    <%if(i%2===0) { %>  
 <section class="container-fluid px-0">
  <div class="row align-items-center mb-3">
    <div class="col-md-4 order-1">
        <img src="<%= produs.imagine.path.replace('/upload', '/upload/w_300')%> " class="img-fluid rounded mb-3" alt="..."
        id="img">
      </div>
      <div class="col-md-8 order-2 mb-2">
        <div class="row justify-content-center">
          <div class="col-lg-10">
            <span class="color fw400 fs-2"><%=produs.nume%></span> 
              <br>
              <span class="color fw400 fs-4"><%=produs.pret%> Lei / <%=produs.gramaj%> </span> <br>
                <span class="color" style="font-size:18px;"><%=produs.descriere%></span> <br>
                <%if(cat.nume === "Lumea lui Espresso " || cat.nume === "Cu Lapte" || cat.nume === "Cafeaua la filtru") {%> 
                  <div class="row">
                    <div class="col-lg-6">
                      <form action="/order/add-to-cart/<%=produs._id%>" method="POST">
                        <label for="cafea" class="color">Alege Cafeaua</label>
                        <select name="cafea" id="cafea" class="color form-select form-select-sm mb-3 bg-transparent" aria-label=".form-select-sm example">
                          <option value="Brazilia0">Brazilia Pedra Grande</option>
                          <option value="Beshasha2">Etiopia Beshasha + 2 Lei</option>
                          <option value="Danisa  3">Etiopia Danisa + 3 Lei</option>
                          <option value="Tarrazu 4">Costa Rica Tarrazu + 4 Lei</option>
                        </select>
                          <button class="btn btn-sm fw400 mb-3 me-3" name ="addButton" >Adaugă</button>   
                          <a href="/meniu/cafea" id="detaliiCafea">Despre cafea!</a>                   
                      </form>
                    </div>
                  </div>
                  <% } else {%>
                    <form action="/order/add-to-cart/<%=produs._id%>" method="POST">
                          <button class="btn btn-sm my-3" name ="addButton" >Adaugă</button>
                     </form>
                 <% } %> 
          </div>
        </div>
      </div>
  </div>
</section>
<% } else{ %> 
  <section class="container-fluid px-0">
    <div class="row align-items-center mb-3">
      <div class="col-md-4 order-1 order-md-2">
          <img src="<%= produs.imagine.path.replace('/upload', '/upload/w_300')%> " class="img-fluid rounded mb-3" alt="..."
          id="img">
        </div>
        <div class="col-md-8 order-2 order-md-1 mb-2">
          <div class="row justify-content-center">
            <div class="col-lg-10">
              <span class="color fw400 fs-2"><%=produs.nume%></span> 
                <br>
                <span class="color fw400 fs-4"><%=produs.pret%> Lei / <%=produs.gramaj%> </span> <br>
                <span class="color" style="font-size:18px;"><%=produs.descriere%></span> <br>
                <%if(cat.nume === "Lumea lui Espresso " || cat.nume === "Cu Lapte" || cat.nume === "Cafeaua la filtru") {%> 
                  <div class="row">
                    <div class="col-lg-6">
                      <form action="/order/add-to-cart/<%=produs._id%>" method="POST">
                        <label for="cafea" class="color">Alege Cafeaua</label>
                        <select name="cafea" id="cafea" class="color form-select form-select-sm mb-3 bg-transparent" aria-label=".form-select-sm example">
                          <option value="Brazilia0">Brazilia Pedra Grande</option>
                          <option value="Beshasha2">Etiopia Beshasha + 2 Lei</option>
                          <option value="Danisa  3">Etiopia Danisa + 3 Lei</option>
                          <option value="Tarrazu 4">Costa Rica Tarrazu + 4 Lei</option>
                        </select>
                          <button class="btn btn-sm fw400 mb-3 me-3" name ="addButton" >Adaugă</button>   
                          <a href="/meniu/cafea" id="detaliiCafea">Despre cafea!</a>                   
                      </form>
                    </div>
                  </div>
                  <% } else {%>
                    <form action="/order/add-to-cart/<%=produs._id%>" method="POST">
                       <!-- <div class="d-grid gap-2"> -->
                          <button class="btn btn-sm my-3" name ="addButton" >Adaugă</button>
                       <!-- </div> -->
                     </form>
                 <% } %> 
            </div>
          </div>
        </div>
    </div>
  </section>
  <% } %> 
<% }) %> 

<!-- <section class="container-fluid px-0">
  <div class="row align-items-center mb-3">
    <div class="col-md-4 order-1 order-md-2">
      <a href="/meniu/cat/<%=cat._id%>">
          <img src="<%= cat.imagine.path.replace('/upload', '/upload/w_600')%> " class="img-fluid rounded mb-3" alt="..."
          id="img">
      </a>
    </div>
      <div class="col-md-8 order-2 order-md-1 mb-2">
        <div class="row justify-content-center">
          <div class="col-lg-10">
            <span class="color fw400 fs-2"><%=cat.nume%></span> 
              <br>
              <span class="color" style="font-size:18px;"><%=cat.descriere%></span> <br>
                    <a href="/meniu/cat/<%=cat._id%>" class="btn btn-sm fw400 my-3" name ="addButton">Descoperă...</a>
                    <% if(autUser && autUser.admin===flowerpower) {%> 
                      <a href="/meniu/cats/<%=cat._id%>/edit"class="color mx-2 my-3" name ="addButton">Modifică</a>
                      <% } %> 
                  
          </div>
        </div>
      </div>
  </div>
</section> -->

  <% }) %> 